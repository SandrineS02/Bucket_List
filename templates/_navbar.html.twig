<nav class="navbar navbar-expand-lg">
    <div class="container-fluid">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('main_home') }}"
                       title="Go back to homepage">
                        Home
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('main_about') }}"
                       title="Go to about us page">
                        A propos
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('wish_list') }}"
                       title="Go to wish list page">
                        Liste de souhaits
                    </a>
                </li>
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('wish_create') }}"
                           title="Add your own ideas!">
                            Ajoutez votre souhait
                        </a>
                    </li>
                {% endif %}
            </ul>
            <ul class="navbar-nav mb-2 mb-lg-0">
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}" title="Logout">
                            Se déconnecter (Bonjour {{ app.user.username }})
                        </a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_register') }}"
                           title="Créer un compte">
                            Créer un compte
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link"  href="{{ path('app_login') }}"
                           title="Se connecter">
                            Se connecter
                        </a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
